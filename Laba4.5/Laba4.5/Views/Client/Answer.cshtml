@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="wIdth=device-wIdth" />
    <title>Answer</title>
</head>
<body>
    <h1>Клиенты</h1>
    <table border="1" wIdth="100%" cellpadding="5">
        <tr>

            <th>Имя</th>
            <th>Фамилия</th>
            <th>Количество ночей</th>
            <th>В каком номере</th>
            <th></th>
            <th></th>
        </tr>
        @{
        foreach (var item in Laba4._5.Managers.Perenos.clients)
        {
        @if (item.lastname.ToLower() == Laba4._5.Managers.Perenos.poisk.ToLower())
        {
    <tr>

        <td>@item.name</td>
        <td>@item.lastname</td>
        <td>@item.nights</td>
        <td>@item.number</td>
        <td><a href="/Clietn/EditClient/@item.Id">Редактировать</a></td>
        <td><a href="/Client/Kick/@item.Id">Удалить</a></td>

    </tr>
        }
        }
        }

    </table>
    @using (Html.BeginForm("CreateClient", "Client"))
    {
    <div>
        <input type="submit" value="Добавить нового клиента" />
    </div>
    }
    <div>
        <a href="/Hotel/ShowHotel">Вернуться к списку отелей</a>
    </div>
</body>
</html>
